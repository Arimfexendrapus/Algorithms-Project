#pragma once

#include <iostream>

using namespace std;


//Exchange sort
template <typename T>
void printArray(T array[], const size_t size)
{
	for (int i = 0; i < size; i++)
	{
		cout << array[i] << endl;
	}
}
